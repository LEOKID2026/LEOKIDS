export default function WorksheetPreviewActions({ onPrint, ui }) {
  return (
    <button type="button" onClick={onPrint} data-testid="worksheet-preview-pdf">
      {ui.downloadPdf}
    </button>
  );
}

{
  "copy": {
    "connect_colors": "ربط الألوان",
    "prototype_connect_pairs_without_crossing": "النموذج الأولي · ربط الأزواج بدون عبور",
    "p_id_colorid_const_isendpoint_endpointmap_cellkey_const_eppair_pairs_fin": "p.id === colorId);\r\n            const isEndpoint = endpointMap[cellKey];\r\n            const epPair = PAIRS.find(\r\n              (p) => keyOf(...p.a) === cellKey || keyOf(...p.b) === مفتاح الخلية،\r\n            );\r\n            معاينة constConflict =\r\n              DragPreview === مفتاح الخلية &&\r\n              اللون النشط &&\r\n              !tryExtend(activeColor, cellKey, paths);\r\n\r\n            يعود (",
    "rect_width_rely_rect_height_return_null_const_c_math_floor_relx_rect_wid": "العرض المستقيم || relY > rect.height) return null;\r\n    const c = الرياضيات.floor((relX / rect.width) * GRID);\r\n    const r = الرياضيات.floor((relY / rect.height) * GRID);\r\n    إذا (ر",
    "clear": "واضح"
  }
}
